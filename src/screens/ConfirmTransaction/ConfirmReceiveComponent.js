import React from 'react';
import PropTypes from 'prop-types';
import {
  View,
  TouchableOpacity,
  StyleSheet,
  StatusBar,
  Dimensions,
  ScrollView
} from 'react-native';
import { Container, Item, Input, Button, Text } from 'native-base';

export default class ConfirmReceive extends React.Component {
  render() {
    return (
      <Container>
        <Text>oi</Text>
      </Container>
    );
  }
}

const styles = StyleSheet.create({
  textCenter: {
    textAlign: 'center',
    fontSize: 12
  }
});
